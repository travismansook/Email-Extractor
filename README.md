# Email-Extractor

•	Built a Bash tool that recursively scans a directory for .txt files and extracts email addresses using regex pattern matching
•	Deduplicated and sorted extracted results, writing a clean output file (unique_emails.txt) from unstructured text data
